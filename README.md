# ANN-Classification-Churn-Model
Customer Churn Prediction using an ANN built with TensorFlow. The project includes data preprocessing with pandas and scikit-learn, feature scaling, and model training. Encoders and scaler are saved for consistent predictions. TensorBoard is used for training visualization
